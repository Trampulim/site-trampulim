# Dependências e build (regenerados automaticamente)
node_modules/
dist/
.astro/

# Sistema
.DS_Store
*.log
npm-debug.log*

# Backups e material de origem (não vão pro site publicado)
backups/
marcas_backup_original/
*.docx

# Config local da ferramenta (pode conter segredos)
.claude/settings.local.json

# PHP legado — vive SÓ na HostGator, não vai pro Netlify.
# anti-spam.php contém o segredo dos tokens; nenhum deles roda na Netlify
# (formulário migrou para Netlify Forms; player usa proxy → HostGator).
public/enviar.php
public/anti-spam.php
public/token.php
public/player/listar.php
